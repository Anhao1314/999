# Integration progress

00 complete: repository, rules, clean worktree, routes, identity/run structures and reference materials inspected. See REPO_AUDIT.md.

01 in progress: isolated domain store, stable seating, HTTP adapter and explicit demo boundary.

Stages are implemented in order; evidence and limitations are updated as checks finish. No commit/push/merge.

01 complete: domain/HTTP adapter created; five deterministic domain tests pass using node --test --test-isolation=none. Windows sandbox denies child-process spawn in default isolation. Live uses full snapshots; contiguous event tests cover demo/future adapter only.
02 complete: same-module lobby shell and native-dialog roster/card structure; no framework or unrelated navigation added.
03 in progress: original DOM pixel room, sprite atlas animation and stable seats.

03 complete: DOM room, three six-seat tables, stable incremental seating, corridor walking, run-driven atlas animation, explicit two-person demo messages and reduced motion.
04 complete: centered native-dialog ID card, separate portrait/sprite, local-only PNG/JPEG/WebP crop with zoom/pan and limits, task metadata and history UI.
05 complete: live unsupported controls disabled; explicit demo exercises versioned next-run configuration, pending/confirmed commands, guarded archive and tombstone. Live enable/assign/start expose separate confirmations.
06 in progress: same-process read projection and bounded persisted history; additive HTTP hook preserves coordinator and schema.

06 complete: module HTTP tests and browser real Kernel assignment → run → persisted activity → artifact → completion verified on explicitly synthetic test records in an isolated test store. No LLM execution claimed. Snapshot revision uses persisted Activity head to reject late older polls; it is not an SSE cursor.
07 in progress: 15 module tests pass; full suite 178 tests: 56 pass / 122 fail (115 existing Windows open-SQLite cleanup errors, 7 existing Windows process exit-code assertions). Both failure classes reproduced from untouched HEAD. Desktop/mobile UI checks found and fixed fetch receiver binding, keyboard focus refresh, dirty-form confirmation and card scroll position. 60 synthetic employees: 10 tables / 60 distinct seat coordinates; offline animations stop.

07 complete: final module suite 16/16 passes; full suite 180 total / 58 pass / 122 existing Windows failures. All employee JS syntax, project check and diff whitespace checks pass. Browser verified native dialog Tab trap, Esc/Back only top layer, origin focus restoration, dirty-form confirmation, portrait import, actual initial snapshot 8 people / 8 unique seats / 0 walking, and 390px card has no horizontal overflow.
08 complete: INTEGRATION_REPORT.md, TEST_RESULTS.md, BACKEND_HANDOFF.md and final 1440/1280/390 screenshots delivered. Real Kernel test loop verified without claiming model execution. Unavailable backend capabilities are explicitly disabled or simulated. Placeholder artwork remains documented. All work left uncommitted; preview uses isolated test data and is stopped after verification.
