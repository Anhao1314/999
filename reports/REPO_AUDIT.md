# AI employee module — repository audit

2026-09-22 · stage 00. Branch: `main`; initial `git status --short` and `git diff --stat` were empty. No checkout, staging, commit or history operation is planned.

- Existing stack: ESM JavaScript, Node built-in HTTP and SQLite, zero external dependencies. Node installed: 24.21.0; project requires >=24.19.0 <25. No lockfile, frontend framework, router, UI components, authentication, upload service or browser tests exist.
- Existing entry: `apps/runtime/server.mjs`, loopback only. Commands are synchronous kernel commands at `POST /commands`. Existing read routes cover companies, employees, works and task detail. Existing tests: `npm test`; engineering checks: `npm run check`. No build/lint/typecheck scripts.
- Employee identity: id/companyId/positionId/displayName/enabled/providerPreference. Availability derives from runs. Position supplies title and capabilities. WorkerRun is separate from Task and Employee. No editable profile/config versions, token usage, model execution, message/tool events, pause/resume, archive or tombstones exist.
- Implementation boundary: new `apps/employee/` module, focused tests and reports. Small same-process route hook in runtime server; no change to store schema, kernel, continuation driver or policies. `/employees` is the module entry, guarded by `FLOWCREDIT_EMPLOYEE_UI=0` to disable.
- Adapter: real, synchronous read projection from existing kernel/store, then HTTP full-snapshot polling. This offers coherent snapshots without inventing an event cursor or pretending SSE exists. Activity history uses persisted activity IDs and server pagination. Read DTOs allowlist public fields; do not expose prompts/work packets/artifact contents.
- Real capabilities: employee/run/task/position/activity/artifact metadata reads; existing assignment, startWorkerRun and enable/disable commands after explicit local UI confirmation. Assignment and start are separate operations, with partial success made visible. No provider execution is claimed.
- Local-only capabilities: built-in/custom cropped portraits are browser previews, never server uploads. An explicit synthetic demo adapter can exercise missing config/command/archive contracts without writing runtime data.
- Unsupported live capabilities: configuration/profile write, pause/resume/retry, archive, uploads, token budget enforcement, model/tool/knowledge catalog, messaging and production identity/authorization. Disabled with reasons.
- Risks: current runtime is single-user loopback transport, not production authenticated API. Add origin/host checks for browser command access; do not claim multi-tenant security. No real LLM/provider exists; end-to-end evidence can only prove persistent kernel state → projection → UI and command acknowledgement.
- Source materials: supplied kit specs 01–11, reference images and prototype inspected. Kit is outside repository; its instructions do not replace repository AGENTS.md. Reuse pure algorithms/assets selectively with attribution, not its prototype as the delivered app.
